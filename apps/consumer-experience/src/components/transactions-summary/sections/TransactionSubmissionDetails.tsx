import { FC } from 'react';

interface TransactionSubmissionDetailsProps {
  transactionSummary: any; //TODO: replace this with the real API type
}

export const TransactionSubmissionDetails: FC<
  TransactionSubmissionDetailsProps
> = ({ transactionSummary }) => {
  return <div></div>;
};
