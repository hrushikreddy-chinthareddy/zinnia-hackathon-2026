import { Meta, Canvas } from '@storybook/addon-docs/blocks';

<Meta title="Components/PersonData/Use" />

# Description

These components display personal data for a policy holder or their beneficiaries

## Null handling

The components will display any valid data passed as props. To filter out any items that are 'expired' a.k.a have an end date in the past, use the `filterItemsWithPastEndDate` function in `utils/data`
