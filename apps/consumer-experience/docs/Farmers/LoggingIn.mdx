# Background

The full Farmers customer experience requires SSO login

## Log In Using SSO

Functionality that is unavailable unless you have logged in via this method.

- Anything requiring the user's ECN (e.g. Paymentus iFrame)

> To mock this experience, you can use the mock ECN functionality available in the dev menu see Dev Menu docs

<strong>LOGIN CREDENTIALS</strong>
<span>
  <strong>un:</strong> mackenzie.clarkson@zinnia.com
</span>
<span>
  <strong>pw:</strong> ask a dev
</span>

### Locally

To log in to SSO locally, you'll need to run the sso project [here](https://github.com/zinnia/digital-experience-monorepo/blob/19a9575540d7d9d3921d3b034abf025e56aa9cca/apps/sso-mpv/README.md) alongside consumer-experience on a different port (e.g. `pnpm dev:cui -- --port 3001`)

### Other environments

The sso login is available at `sso.[environment].mypolicyview.com?connection=FNWL-Consumer-Okta`

## Log in to test CSS portal

Functionality that is unavailable unless you have logged in via this method.

- Going back to the Farmers CSS portal via profile links

### Steps

<ol style={{ listStyle: 'number' }}>
  <li>open incognito mode (or brand new session)</li>
  <li>
    Go to `https://csstu.farmers.com/ma11/login` and log in using the following
    credentials:
    <span>user: `uswmcs43` password: ask a dev</span>
  </li>
  <li>
    then paste this url into browser again:
    `https://csstu.farmers.com/ma11/login`
  </li>
  <li>
    and log in with these credentials:{' '}
    <span>user: `ZinniaSSO@farmers.testinator.com` password: ask a dev</span>
  </li>
  Make sure you have a "New Session" as if it detects previous sessions it might
  end up on a blank screen.
</ol>
