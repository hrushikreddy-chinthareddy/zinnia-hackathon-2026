import { Meta } from '@storybook/blocks';
import { Address } from '../../components/address';

<Meta title="Components/Address/Use" />

# Let's Address This Component

The address component is meant to standardize the structure of the address, but the formatting is up to the consumer.
Thus, all address values should be passed in as formatted strings.

### TODO:

- Allow user to update font family and sizing
