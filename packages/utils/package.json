{
  "name": "@zinnia/utils",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "pnpm run watch",
    "watch": "tsup --watch",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "postinstall": "pnpm run build"
  },
  "jest": {
    "preset": "@zinnia/jest-presets/jest/node"
  },
  "dependencies": {
    "@zinnia/api-types": "workspace:*"
  },
  "devDependencies": {
    "@zinnia/eslint-config": "workspace:*",
    "@zinnia/jest-presets": "workspace:*",
    "@zinnia/typescript-config": "workspace:*",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.24",
    "jest": "^29.7.0",
    "tsup": "^8.0.2",
    "typescript": "^5.3.3"
  }
}
