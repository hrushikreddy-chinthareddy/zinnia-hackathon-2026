name: 'Consumer Experience Dev to Dev'

on:
  workflow_dispatch:

jobs:
  dev-deploy:
    uses: ./.github/workflows/deploy-consumer-experience-action.yml
    with:
      environment-level: dev
    secrets: inherit
