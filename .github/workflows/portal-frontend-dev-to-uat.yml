name: 'Portal Frontend Dev to UAT'

on:
  workflow_dispatch:

jobs:
  dev-deploy-east:
    if: github.ref_name == 'dev'
    uses: ./.github/workflows/deploy-portal-frontend-east.yml
    with:
      environment-level: dev
    secrets: inherit
