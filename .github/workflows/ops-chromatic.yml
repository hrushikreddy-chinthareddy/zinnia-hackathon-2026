name: 'Chromatic OPS'

# TODO: Renanble it to workflow_call when we fix the js memory issue
on:
  workflow_dispatch:
  # push:
  # paths:
  # - 'apps/ops/**'
  # branches-ignore:
  # - 'dependabot/**'

jobs:
  chromatic:
    uses: './.github/workflows/build-chromatic.yml'
    with:
      project-name: OPS
      filter-path: ops
      working-dir: apps/ops
    secrets:
      chromatic-token: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
