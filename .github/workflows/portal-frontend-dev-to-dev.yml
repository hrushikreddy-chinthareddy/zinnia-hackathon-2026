name: 'Portal Frontend Dev to Dev'

on:
  workflow_dispatch:

jobs:
  dev-deploy-east:
    uses: ./.github/workflows/deploy-portal-frontend-east.yml
    with:
      environment-level: dev
    secrets: inherit
